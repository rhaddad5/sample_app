<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <div class="user-info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </div>
    <%= will_paginate @microposts %>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
  </aside>
</div>
