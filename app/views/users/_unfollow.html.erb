<%= simple_form_for current_user.active_relationships.find_by(followed_id: @user.id), method: :delete, remote: true do |f| %>
  <%= f.button :submit, "Unfollow", class: "btn btn-primary" %>
<% end %>
